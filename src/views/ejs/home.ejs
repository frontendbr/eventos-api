<% if (!user) { %>
	<h2>Welcome! Please log in.</h2>
<% } else { %>
	<h2>Hello, <%= user.displayName %>.</h2>
	<script>
		var token = '<%= user.token.accessToken %>';
		console.log(token);
	</script>
<% } %>
